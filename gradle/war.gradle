apply plugin: "war"

bootWar {
    mainClass = "com.smburu.employee.EmployeeManagementApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
